# Указываем базовый image, с которого мы начинаем сборку контейнера
FROM node:13.12.0-alpine

# Указываем с какого каталога начинать работу внутри контейнера
WORKDIR /usr/src/app

# Копируем файлы package*.json с хоста - в корневую директорию контейнера 
COPY package*.json ./

# Указываем, какую команду необходимо запустить внутри контейнера
RUN npm i

# Копируем все файлы проекта с хоста - в корневую директорию контейнера
COPY . .

# 2 комманды ниже нужны только для prod
# RUN npm run build
# RUN npm i -g serve